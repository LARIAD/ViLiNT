# Branch description

This branch is a minimal research-style project page scaffold for **Multimodal Navigation Transformer**.

The page is data-driven: `static/js/index.js` reads `content/site.json` and fills in the sections automatically.

